#include <iostream>
using namespace std;
int my_min(int x, int y) {
return (x < y) ? x : y;
}
int main()
{
int a, b, x, y;
cout << "Enter a: ";
cin >> a;
cout << "Enter b: ";
cin >> b;
x = my_min(a, 3*b);
y = my_min(2*a-b, 26);
int z = my_min(x, y);
cout << "z = " << z << endl;
return 0;
}
