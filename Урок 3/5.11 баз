#include <iostream>
#include <cmath>
using namespace std;
int main()
{
const float R = 6350; 
for(int h=1; h<=10; h++)
{
float d = sqrt(pow(R+h,2)-pow(R,2)); 
cout « "Высота: " « h « " км, расстояние до горизонта: " « d « " км" « endl;
}
return 0;
}
