#include <iostream>
using namespace std;
int main() {
    int n;
    cout << "Введите целое число N>0: ";
    cin >> n;
    cout << "Цифры числа N: ";
    while (n > 0) {
        cout << n % 10 << " ";
        n /= 10;
    }
    cout << endl;
    return 0;
}
