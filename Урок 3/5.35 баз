#include <iostream>
using namespace std;
int main() {
    int n;
    double sum = 0;
    cout << "Введите n: ";
    cin >> n;
    for (int i = 1; i <= n; i++) {
        sum += pow(-1, i+1) * (1.0/i);
    }
    cout << "Сумма: " << sum << endl;
    return 0;
}
