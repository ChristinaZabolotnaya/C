#include <iostream>
#include <cmath>

using namespace std;

int main() {
    
    double a, b, avg;
    
    cout << "Введите два числа: ";
    cin >> a >> b;
    
    avg = (a + b) / 2;
    
    cout << "Среднее арифметическое этих чисел: " << avg << endl;
    
    return 0;
}
