#include <iostream>
#include <cmath>
using namespace std;

int main() {
   double a, x;

   cout << "Введите значение a: ";
   cin >> a;

   x = sqrt((2 * a + sin(abs(3 * a))) / 3.56);

   cout << "Значение функции x = sqrt((2*a+sin(модуль(3a)))/3,56) равно " << x << endl;

   return 0;
}
