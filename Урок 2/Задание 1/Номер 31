#include <iostream>

using namespace std;

int main() {
    int a, b, c;
    cin >> a >> b >> c;
    
    if ( a == b || b == c || a == c ) { // Треугольник со сторонами а, b, c является равнобедренным
        cout << "True";
    } 
    
    else {
        cout << "False";
    }
    
    return 0;
}
