#include <iostream>

using namespace std;

int main() {
   
    int a, b, c;
    cin >> a >> b >> c;
    
    if (a < b && b < c || a > b && b > c) { // Число В находится между числами А и С
        cout << "True";
    } 
    
    else {
        cout << "False";
    }
    
    return 0;
}
