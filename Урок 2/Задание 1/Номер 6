#include <iostream>

using namespace std;

int main() {
   
    int a, b, c;
    cin >> a >> b >> c;
    
    if (a < b && b < c) { // Справедливо двойное неравенство А < B < C
        cout << "True";
    } 
    
    else {
        cout << "False";
    }
    
    return 0;
}
