#include <iostream>

using namespace std;

int main() {
   
    int a;
    cin >> a;
   
    if (a % 2 == 0) { // Число А является четным
        cout << "True";
    } 
    
    else {
        cout << "False";
    }
    
    return 0;
}
