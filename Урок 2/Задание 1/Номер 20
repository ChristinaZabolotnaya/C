#include <iostream>

using namespace std;

int main() {
   
    int n, a, b, c;
    cin >> n;
    
    a = n / 100;
    b = (n / 10) % 10;
    c = n % 10;
    
    if (a != b && a != c && b != c) { // Все цифры данного числа различны
        cout << "True";
    } 
    
    else {
        cout << "False";
    }
    
    return 0;
}
