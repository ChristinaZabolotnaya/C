#include <iostream>

using namespace std;

int main() {
    
    int a, b, c;
    cin >> a >> b >> c;
    
    if (a <= b && a <= c) {
        cout << a << endl;
        if (b >= c) {
            cout << b;
        } 
        else {
            cout << c;
        }
    } 
    
    else { 
        if (b <= a && b <= c) {
            cout << b << endl;
        if (a >= c) {
            cout << a;
        } 
        else {
            cout << c;
            }
        } 
        else {
            cout << c << endl;
            if (a >= b) {
                cout << a;
            }
            else {
                cout << b;
            }
        }
    }
    
    return 0;
}
